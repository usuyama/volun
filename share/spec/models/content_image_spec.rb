require 'spec_helper'

describe ContentImage do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    ContentImage.create!(@valid_attributes)
  end
end
