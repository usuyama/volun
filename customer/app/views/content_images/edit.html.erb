<h1>Editing content_image</h1>

<% form_for(@content_image) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @content_image %> |
<%= link_to 'Back', content_images_path %>